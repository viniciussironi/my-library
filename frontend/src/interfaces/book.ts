export interface Book {
    id: string;
    title: string;
    author: string;
    totalPages: number;           
    location: string;       
    createdAt: string;
    updatedAt: string;
    fileType: string;
}